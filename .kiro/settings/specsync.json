{
  "auto_remediation": {
    "enabled": true,
    "mode": "tasks",
    "description": "Mode options: 'blocking' (block commit on drift), 'tasks' (generate task list), 'semi-auto' (prompt user to ask Kiro for fixes)"
  },
  "semi_auto_fix": {
    "enabled": false,
    "require_user_invocation": true,
    "create_separate_commit": true,
    "commit_message_template": "🤖 SpecSync: Auto-fix drift for {original_commit}",
    "description": "Semi-automatic mode - User must manually ask Kiro to fix drift. Kiro then fixes everything and commits automatically."
  },
  "validation": {
    "block_on_drift": false,
    "allow_commit_with_tasks": true,
    "check_spec_alignment": true,
    "check_test_coverage": true,
    "check_documentation": true,
    "check_bridge_contracts": true,
    "description": "Toggle which validations to run. Set to false to disable specific checks."
  },
  "task_generation": {
    "create_in_spec_folder": true,
    "file_name": "remediation-tasks.md",
    "include_priority": true,
    "include_timestamps": true
  },
  "notification": {
    "show_task_summary": true,
    "open_tasks_file": false,
    "notify_on_semi_auto": true
  },
  "git": {
    "preserve_original_commit": true,
    "create_fixup_commit": false,
    "auto_push": false,
    "description": "Git behavior for semi-auto mode"
  }
}
